# 🚀 Инициализация React Native проекта

## Проблема
У вас нет папки `android` потому что React Native проект еще не инициализирован с нативными частями.

## ✅ Решение: Инициализация проекта

### Вариант 1: Инициализация нового проекта (Рекомендуется)

```bash
# 1. Перейдите на уровень выше
cd /home/user

# 2. Создайте новый React Native проект
npx react-native@latest init MindPeace --template react-native-template-typescript

# 3. Скопируйте наши файлы в новый проект
# Скопируйте src/
cp -r meditation/src MindPeace/

# Скопируйте package.json (зависимости)
cp meditation/package.json MindPeace/package.json

# Скопируйте конфигурационные файлы
cp meditation/tsconfig.json MindPeace/tsconfig.json

# Скопируйте документацию
cp meditation/*.md MindPeace/

# 4. Перейдите в новый проект
cd MindPeace

# 5. Установите зависимости
npm install

# 6. Установите под-зависимости для Android
cd android
./gradlew clean
cd ..

# 7. Запустите проект
npx react-native run-android
```

### Вариант 2: Добавить нативные части к существующему проекту

Если вы хотите сохранить текущую структуру:

```bash
# 1. Перейдите в проект
cd /home/user/meditation

# 2. Установите React Native CLI глобально
npm install -g react-native-cli

# 3. Инициализируйте только нативные части
# (К сожалению, это не поддерживается напрямую)
# Лучше использовать Вариант 1
```

---

## 🎯 Быстрый старт (Пошагово)

Я помогу вам выполнить инициализацию. Следуйте инструкциям:

### Шаг 1: Создайте новый React Native проект

```bash
cd /home/user
npx react-native@latest init MindPeace --template react-native-template-typescript
```

**Это займет 5-10 минут**. Будут установлены:
- React Native
- Android и iOS нативные части
- Зависимости
- Базовая структура

### Шаг 2: Скопируйте наши файлы

После создания проекта MindPeace выполните:

```bash
# Удалите стандартный src (если есть)
rm -rf MindPeace/src

# Скопируйте наш src
cp -r meditation/src MindPeace/

# Обновите package.json
cp meditation/package.json MindPeace/package.json

# Скопируйте tsconfig
cp meditation/tsconfig.json MindPeace/tsconfig.json

# Скопируйте всю документацию
cp meditation/CONCEPT.md MindPeace/
cp meditation/ARCHITECTURE.md MindPeace/
cp meditation/DESIGN.md MindPeace/
cp meditation/BUILD_GUIDE.md MindPeace/
cp meditation/USAGE_EXAMPLES.md MindPeace/
cp meditation/README.md MindPeace/
```

### Шаг 3: Установите зависимости

```bash
cd MindPeace
npm install
```

### Шаг 4: Настройте Android

```bash
cd android
./gradlew clean
cd ..
```

### Шаг 5: Обновите App.tsx

Создайте главный файл приложения:

```bash
# В корне проекта MindPeace создайте App.tsx
```

Содержимое `App.tsx`:

```typescript
import React from 'react';
import AppNavigator from './src/navigation/AppNavigator';

const App = () => {
  return <AppNavigator />;
};

export default App;
```

### Шаг 6: Запустите приложение

```bash
# Запустите Metro bundler
npx react-native start

# В другом терминале запустите Android
npx react-native run-android
```

---

## 📁 Структура после инициализации

```
MindPeace/
├── android/                 ← Появится после инициализации
│   ├── app/
│   │   ├── src/
│   │   └── build.gradle
│   ├── gradle/
│   └── build.gradle
├── ios/                     ← iOS часть
├── src/                     ← Наш код
│   ├── components/
│   ├── screens/
│   ├── theme/
│   ├── data/
│   └── navigation/
├── App.tsx                  ← Главный файл
├── package.json
├── tsconfig.json
└── *.md                     ← Документация
```

---

## 🔧 Альтернатива: Expo (Проще для начинающих)

Если React Native кажется сложным, можно использовать Expo:

### Инициализация с Expo

```bash
# 1. Установите Expo CLI
npm install -g expo-cli

# 2. Создайте проект
cd /home/user
npx create-expo-app MindPeaceExpo --template expo-template-blank-typescript

# 3. Скопируйте файлы
cp -r meditation/src MindPeaceExpo/
cp meditation/tsconfig.json MindPeaceExpo/

# 4. Установите зависимости
cd MindPeaceExpo
npm install

# 5. Запустите
npx expo start
```

### Преимущества Expo:
- ✅ Проще в настройке
- ✅ Не нужен Android Studio для разработки
- ✅ Можно тестировать через приложение Expo Go
- ✅ Легче собрать APK

### Недостатки Expo:
- ❌ Больше размер APK
- ❌ Меньше контроля над нативным кодом
- ❌ Некоторые библиотеки могут не работать

---

## 🎯 Что делать дальше?

### Вариант А: Использовать React Native (Рекомендуется для продакшн)

1. Выполните **Шаг 1-6** выше
2. У вас появится папка `android/`
3. Следуйте **BUILD_GUIDE.md**

### Вариант Б: Использовать Expo (Быстрее для прототипа)

1. Выполните инициализацию Expo
2. Соберите APK через:
   ```bash
   eas build --platform android --profile preview
   ```

---

## ⚡ Быстрая команда для копирования

После создания проекта MindPeace:

```bash
#!/bin/bash

# Перейдите в родительскую директорию
cd /home/user

# Скопируйте все наши файлы
cp -r meditation/src MindPeace/
cp meditation/package.json MindPeace/
cp meditation/tsconfig.json MindPeace/
cp meditation/*.md MindPeace/

# Перейдите в новый проект
cd MindPeace

# Установите зависимости
npm install

# Очистите Android
cd android && ./gradlew clean && cd ..

echo "✅ Проект готов! Теперь запустите: npx react-native run-android"
```

Сохраните это как `copy-files.sh` и выполните:

```bash
chmod +x copy-files.sh
./copy-files.sh
```

---

## 🆘 Если возникли проблемы

### Проблема: npx react-native init зависает

**Решение:**
```bash
# Используйте yarn вместо npm
yarn global add react-native-cli
react-native init MindPeace --template react-native-template-typescript
```

### Проблема: Ошибка при установке зависимостей

**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и lock файлы
rm -rf node_modules package-lock.json

# Установите заново
npm install
```

### Проблема: Android SDK не найден

**Решение:**
1. Установите Android Studio
2. Откройте Android Studio
3. Tools > SDK Manager
4. Установите SDK Platform 33
5. Настройте ANDROID_HOME (см. BUILD_GUIDE.md)

---

## 📋 Чек-лист

- [ ] Установлен Node.js (18+)
- [ ] Установлен Android Studio
- [ ] Настроен ANDROID_HOME
- [ ] Выполнен `npx react-native init MindPeace`
- [ ] Скопированы файлы из meditation/
- [ ] Выполнен `npm install`
- [ ] Папка `android/` существует
- [ ] Можно запустить `npx react-native run-android`

---

**После выполнения этих шагов у вас появится папка `android/` и можно будет собирать APK!** 🚀
