# 🔧 Исправление ошибки Gradle Sync

## ❌ Ошибка:
```
Settings file 'D:\Android\meditation\android\settings.gradle' line: 3
A problem occurred evaluating settings 'android'.
> Process 'command 'node'' finished with non-zero exit value 1
```

## ✅ Причина:
Gradle не может выполнить Node.js команды, потому что не установлены npm зависимости проекта.

---

## 🔨 Решение:

### Вариант 1: Установка через npm (рекомендуется)

#### 1. Откройте терминал (PowerShell, CMD, или Terminal)

#### 2. Перейдите в корневую папку проекта:
```bash
cd D:\Android\meditation
```

#### 3. Проверьте наличие Node.js:
```bash
node --version
npm --version
```

Должно показать версии, например:
```
v20.10.0
10.2.3
```

**Если Node.js не установлен:**
- Скачайте с https://nodejs.org (рекомендуется LTS версия)
- Установите и перезапустите терминал

#### 4. Установите зависимости:
```bash
npm install
```

Это займёт **5-10 минут**. Будет создана папка `node_modules` с тысячами файлов.

#### 5. Проверьте что установка успешна:
```bash
ls node_modules
```

Должно показать множество папок, включая:
- expo
- react
- react-native
- @react-navigation
- и другие

---

### Вариант 2: Установка через Yarn (альтернатива)

Если у вас установлен Yarn:

```bash
cd D:\Android\meditation
yarn install
```

---

## 🎯 После установки зависимостей:

### Шаг 1: Вернитесь в Android Studio

### Шаг 2: Синхронизируйте проект
- **File → Sync Project with Gradle Files**
- Или нажмите иконку 🐘 с синхронизацией справа вверху

### Шаг 3: Дождитесь завершения Gradle Sync
Внизу должно появиться:
```
BUILD SUCCESSFUL in Xs
```

### Шаг 4: Теперь можно собирать APK! 🎉
- **Build → Build Bundle(s) / APK(s) → Build APK(s)**

---

## 📂 Правильная структура проекта после установки:

```
D:\Android\meditation\
├── android\              ← открываете ЭТУ папку в Android Studio
│   ├── app\
│   ├── gradle\
│   ├── build.gradle
│   └── settings.gradle
├── src\                  ← исходный код React Native
├── node_modules\         ← ✅ ДОЛЖНА ПОЯВИТЬСЯ после npm install!
├── package.json
├── package-lock.json     ← создаётся после npm install
└── app.json
```

---

## ❗ Частые проблемы:

### Проблема 1: "node is not recognized"
**Причина:** Node.js не установлен или не в PATH

**Решение:**
1. Скачайте Node.js с https://nodejs.org
2. Установите с опцией "Add to PATH"
3. Перезапустите терминал
4. Проверьте: `node --version`

---

### Проблема 2: "npm install" зависает или очень медленно
**Причина:** Медленное интернет соединение или проблемы с npm registry

**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Попробуйте снова
npm install
```

---

### Проблема 3: После npm install всё равно ошибка Gradle Sync
**Причина:** Gradle не видит новые файлы

**Решение:**
1. В Android Studio: **File → Invalidate Caches → Invalidate and Restart**
2. После перезапуска: **File → Sync Project with Gradle Files**

---

### Проблема 4: Ошибка "EACCES" или "permission denied"
**Причина:** Нет прав на запись в папку

**Решение (Windows):**
- Запустите терминал от имени администратора
- Или переместите проект в папку без ограничений (например, `C:\Projects\meditation`)

---

## 🚀 Быстрая команда для опытных:

```bash
cd D:\Android\meditation && npm install && cd android && gradlew clean
```

После этого в Android Studio: File → Sync Project with Gradle Files

---

## ✅ Контрольный чеклист:

Перед открытием в Android Studio убедитесь:

- [ ] Node.js установлен (`node --version` работает)
- [ ] npm установлен (`npm --version` работает)
- [ ] Выполнена команда `npm install` в корневой папке проекта
- [ ] Появилась папка `node_modules` (размер ~500-800 MB)
- [ ] Есть файл `package-lock.json`
- [ ] В Android Studio открываете папку `meditation\android`, а не `meditation`

После этого Gradle Sync должен пройти успешно! 🎊

---

## 💡 Совет:

В будущем, если будете создавать новый проект или клонировать из Git:

1. **Сначала:** `npm install` в корневой папке
2. **Потом:** открывайте Android Studio

Это правильная последовательность для любого React Native/Expo проекта!
